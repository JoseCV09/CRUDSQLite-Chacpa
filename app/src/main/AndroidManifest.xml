<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.yoandroid.crudsqlite_chacpa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="xyz.yoandroid.crudsqlite_chacpa.View.DetailAlumno"
            android:configChanges="orientation|screenSize"
            android:parentActivityName="xyz.yoandroid.crudsqlite_chacpa.View.MainActivity"
            android:windowSoftInputMode="adjustResize|stateHidden"
            >

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="xyz.yoandroid.crudsqlite_chacpa.View.MainActivity" />

        </activity>
        <activity android:name="xyz.yoandroid.crudsqlite_chacpa.View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>